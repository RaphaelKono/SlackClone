import { PeriodOfTimePipe } from './period-of-time.pipe';

describe('PeriodOfTimePipe', () => {
  it('create an instance', () => {
    const pipe = new PeriodOfTimePipe();
    expect(pipe).toBeTruthy();
  });
});
