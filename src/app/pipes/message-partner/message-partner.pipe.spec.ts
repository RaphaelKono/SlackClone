import { MessagePartnerPipe } from './message-partner.pipe';

describe('MessagePartnerPipe', () => {
  it('create an instance', () => {
    const pipe = new MessagePartnerPipe();
    expect(pipe).toBeTruthy();
  });
});
